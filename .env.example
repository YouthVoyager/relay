# 复制为 .env 并按需填写。.env 已被 gitignore,永远不要提交真实密钥。
RELAY_ADDR=:8080
RELAY_DB_PATH=relay.db
LLM_BASE_URL=https://tokenhub.tencentmaas.com/v1
LLM_API_KEY=sk-*****
LLM_MODEL=hy3
# 上下文压缩阈值(prompt tokens),建议为模型窗口的 50%~70%
RELAY_COMPACTION_THRESHOLD=20000